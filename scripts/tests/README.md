# scripts tests

## Purpose

Cross-cutting tests for the `scripts/` tree.

Command-specific tests live beside their command under each command's `tests/` directory.

## Usage

```bash
bash test-script-structure.sh
```
