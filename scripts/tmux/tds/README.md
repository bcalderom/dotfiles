# tds

## Purpose

Open a selected directory in a tmux development session layout.

## Usage

```bash
tds
tds --root ~/Desarrollos
```

## Dependencies

Requires `tmux`; interactive selection uses `fzf`.

## Tests

```bash
bash tests/test-tds.sh
```
